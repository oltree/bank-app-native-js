import { ScreenComponent } from '@/core/component';

export class AboutUs extends ScreenComponent {
  constructor() {
    super({ title: 'About us' });
  }

  render() {
    return '<p>About-us</p>';
  }
}
